
import React from 'react';
import { LensSimulatorMain } from '@/components/LensSimulator/LensSimulatorMain';

const LensSimulatorPage = () => {
  return <LensSimulatorMain />;
};

export default LensSimulatorPage;
